[project]
name = "janesthetic"
dynamic = ["version"]
description = "JAX-anesthetic"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jax",
    "chex",
]

[build-system]
requires = ["setuptools_scm>=8"]

[tool.setuptools_scm]
version_file = "janesthetic/_version.py"

[project.optional-dependencies]
dev = ["anesthetic", "blackjax>=1.6", "pytest", "pytest-cov"]

[tool.setuptools.packages.find]
include = ["janesthetic"]
